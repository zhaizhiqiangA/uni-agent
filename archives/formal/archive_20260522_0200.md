====================================
【开发存档】2026-05-22 02:00
====================================
本次存档目的：
验证驱动的迭代开发——完成静态验证 + 集成验证，发现并修复集成 bug，编写 RFC 和测试套件。

已修改文件列表：
- examples/swe_agent_blackbox/parallel_infer.py：删除无效的 `host` kwarg（GatewayActor 不接受此参数）
- examples/swe_agent_blackbox/agent_runner.py：修复 deployment 缺少 `type` discriminator（`deployment.setdefault("type", "local")`）；timeout_budget 默认值从 -1 改为 300
- examples/swe_agent_blackbox/scripts/run_infer.sh：添加 RUNNER 环境变量 + `--runner` 参数传递
- examples/swe_agent_blackbox/docs/VERIFICATION.md：修正 #12 Trajectory 字段描述；标记 #25/#45（runner 传递）和 #46（timeout_budget）为已修复
- docs/rfcs/0001-blackbox-agent-integration.md：新增 Blackbox Agent Integration RFC，描述通用第三方 agent 接入训练方案
- tests/test_swe_agent_blackbox.py：新增 4 层测试套件（47 tests），覆盖单元模块 → 数据集加载 → 推理端到端 → 训练端到端

Git Commit 信息：
commit 号：5d0f121
commit 描述：feat(swe_agent_blackbox): add RFC, test suite, and fix integration bugs

核心修改内容：
从上一次存档（refactor 006ef34）至今，主要工作为验证驱动：

1. 静态验证 #1-#32 全部通过（模块 import、类型签名、reward 数据流、config 文件、脚本、逻辑审查）
2. 编写 47 项测试用例，按 VERIFICATION.md 层级组织为 4 个 Tier：
   - Tier 1（41 tests, no GPU）：模块 import、reward context、compute_score、reward spec、agent config、tool parser、image remap、Hydra config、FQN 引用
   - Tier 2（数据集加载）：SWE-bench/R2E-Gym parquet 加载 + 镜像映射
   - Tier 3（推理端到端，GPU）：uniagent/mini_swe runner 推理
   - Tier 4（训练端到端，manual）：训练 + reward 曲线 + checkpoint
3. 集成验证中发现 3 个 bug 并修复：
   - `parallel_infer.py` 传了 GatewayActor 不接受的 `host` 参数 → 删除
   - `_create_agent_env` 在 agent_config 为空时 deployment 缺少 `type` 字段 → setdefault
   - `timeout_budget=-1` 实际语义是"timeout 即终止"而非"无限" → 默认值改为 300
4. 新增 RFC 文档，面向外部用户，泛化描述通用黑盒 agent 接入训练方案：
   - 明确 gateway-interception 架构：gateway 拦截 LLM 调用收集 token trajectories
   - Reward 分 Mode A（标准，compute_score 计算）和 Mode B（可选，runner 内计算）
   - 提供完整集成指南（6 步）和 runner checklist

Tier 1+2 测试 41/41 通过。Tier 3 推理测试因 Docker sandbox 创建失败（环境问题，非代码 bug）未通过，需后续验证。
====================================
